# Fail2ban filter for nginx rate limit violations
# Matches when clients hit the rate limit (429 errors)

[Definition]
failregex = limiting requests, excess:.* by zone.*client: <HOST>
            
ignoreregex =
